# User–AI Interaction Report — agents-playground

**Date:** 2026-06-22
**Period covered:** 2026-06-04 → 2026-06-22
**Sources:** Prompt history (`agent-transcripts/`). The `docs/ai-interaction-logs/` folder was removed since the last report, so this is reconstructed from your prompts and the resulting work.
**Scope:** From MSW mock flag through deployment (Vercel/Neon CI), per-device personalization, the analytics REST API, Swagger docs, structured logging, and a standalone analytics MCP server.

## Summary

This period shifted from building features to **shipping and operating** the app:
deployment, observability, personalization, an analytics API, and an MCP server.
Your style stayed **delegate-then-steer with tight specs**: you almost always let
the AI brainstorm a plan, approved it ("approved" / "go ahead" / "confirmed"),
then issued precise corrections on scope, libraries, and infra versions. You
approved most plans but **trimmed scope and overrode several technical choices**.

## Accepted Suggestions

| Area                | What you accepted                                                          | Why it stuck                                                 |
| ------------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------ |
| MSW flag            | Env-var toggle to serve mocked data via MSW                                | Clean local/dev switch without touching prod paths.          |
| Geolocation default | 5-day forecast + geolocate initial city, fall back to first favorite       | Matched the brainstormed plan; "approved" as proposed.       |
| CI/CD               | GitHub Actions for Vercel + Neon, split into app / DB-migrations / both    | You expanded on the AI's plan and "confirmed".               |
| Personalization     | Device-ID tracking, favorites + city loads per device, Prisma schema/tests | Plan-driven; implemented as specified.                       |
| Analytics API       | Single `POST /api/analytics`, unified `{metric, from, to, limit}` schema   | Your spec; AI added 2 extra metrics on request, "it's good". |
| Docs                | Swagger UI for the REST APIs                                               | Useful contributor-facing surface; "approved".               |
| Logging             | pino as the Next.js logging lib, DB queries at debug, debug default        | Lightweight best-fit choice; "go ahead".                     |
| MCP server          | Standalone analytics MCP server + `analytics:debug` script, doc comments   | Improves contributor onboarding/troubleshooting.             |

## Rejected / Corrected Suggestions

Real pushbacks and overrides from your prompts (not AI-internal alternatives).

| #   | AI did / proposed                                                 | Your response                                                                              | Why                                                                          |
| --- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------- |
| 1   | Built toward a 14-day forecast via OpenWeather One Call API 4.0   | Hit `401`, then next day "update UI to show 5-day forecast instead"                        | One Call 4.0 needs a paid subscription; reverted to the free 5-day endpoint. |
| 2   | Added e2e tests + related config during deploy work               | "let's remove e2e test and related config changes, I do not need so far"                   | Out of scope for now; kept the change set lean.                              |
| 3   | Assumed/needed auth + multiple endpoints + defaults for analytics | "publicly accessible for now"; "all intervals required, no defaults, single POST endpoint" | Wanted one simple, fully-parametrized public endpoint.                       |
| 4   | Public `favoritesPerDevice` exposed raw device IDs                | Caught in final review → replaced with `device-1/2…` labels                                | Privacy: don't leak long-lived cookie IDs from a public endpoint.            |
| 5   | Hand-rolled cookie handling for device ID                         | "use a trusted lib to work with cookies"                                                   | Prefer a vetted dependency over custom cookie code.                          |
| 6   | MCP server with stdio + local validation transport                | "no local with Zod needed - return HTTP error"; "leave only HTTP transport"                | Simplify to a single HTTP transport returning HTTP errors.                   |
| 7   | Node 26 build target for TS                                       | "downgrade target node for TS build to v24"                                                | Align build target with the supported/runtime version.                       |

## How You Steered the AI

- **Approved via brainstormed plans**, not open-ended go-aheads ("approved", "confirmed", "it's good", numbered option picks).
- **Tightened scope aggressively**: removed e2e tests, narrowed MCP to "analytics only" and HTTP-only transport, demanded a single no-defaults endpoint.
- **Specified libraries/versions explicitly**: trusted cookie lib, pino, Node 24 target, One Call API.
- **Drove iteratively through real errors**: 401 weather auth, `mkdir /var/task/data`, `PrismaClient` init/type errors, deprecated `Server`, SwaggerUI runtime error.

## Notes / Risks

- The 14-day forecast goal was dropped due to API tier limits; current UI is 5-day.
- Several deploy-time fixes (serverless `data/` writes, Prisma init on Vercel) indicate the file-based favorites store from the prior phase needed DB-backed/runtime adjustments in production.
- The analytics endpoint is intentionally public for now; auth was deferred, with privacy mitigated via labeled device identifiers and capped limits/intervals.
