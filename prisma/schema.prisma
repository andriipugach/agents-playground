generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model FavoriteCity {
  id        Int      @id @default(autoincrement())
  city      String   @unique
  createdAt DateTime @default(now())
}
